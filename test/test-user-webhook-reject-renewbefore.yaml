apiVersion: auth.openkube.io/v1alpha1
kind: User
metadata:
  name: aggressive-renewal-user
spec:
  auth:
    type: x509
    ttl: "720h"  # 30 days
    renewBefore: "660h"  # WILL BE REJECTED: 91.7% of TTL, exceeds 90% cap
    autoRenew: true
  clusterRoles:
    - existingClusterRole: view
