apiVersion: auth.openkube.io/v1alpha1
kind: User
metadata:
  name: short-ttl-user
spec:
  auth:
    type: x509
    ttl: "24h"  # WILL BE REJECTED: Less than 24h minimum in production
    autoRenew: true
  clusterRoles:
    - existingClusterRole: view
