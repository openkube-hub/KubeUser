apiVersion: auth.openkube.io/v1alpha1
kind: User
metadata:
  name: jane
spec:
  auth:
    type: x509
    ttl: 24h
  roles:
    - namespace: dev
      existingRole: tester
  clusterRoles:
    - existingClusterRole: edit