apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "kubeuser.fullname" . }}-controller-manager
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "kubeuser.managerLabels" . | nindent 4 }}
  {{- with .Values.podLabels }}
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "kubeuser.managerSelectorLabels" . | nindent 6 }}
  template:
    metadata:
      annotations:
        kubectl.kubernetes.io/default-container: manager
        {{- with .Values.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      labels:
        {{- include "kubeuser.managerSelectorLabels" . | nindent 8 }}
        {{- with .Values.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "kubeuser.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
      - name: manager
        image: {{ include "kubeuser.image" . }}
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        command:
        - /manager
        args:
        {{- range .Values.manager.args }}
        - {{ . }}
        {{- end }}
        {{- if .Values.metrics.enabled }}
        - --metrics-bind-address=:{{ .Values.metrics.service.port }}
        - --metrics-secure={{ .Values.metrics.secure }}
        {{- if and .Values.metrics.secure .Values.metrics.certManager.enabled }}
        - --metrics-cert-path=/tmp/k8s-metrics-server/serving-certs
        {{- end }}
        {{- end }}
        env:
        - name: WEBHOOK_SERVICE_NAME
          value: {{ include "kubeuser.fullname" . }}-webhook-service
        - name: KUBEUSER_NAMESPACE
          value: {{ .Release.Namespace }}
        - name: KUBEUSER_SIGNER_NAME
          value: {{ (.Values.signerName | default "kubernetes.io/kube-apiserver-client") | quote }}
        - name: KUBEUSER_DEFAULT_TTL
          value: {{ .Values.authDefaults.ttl | quote }}
        - name: KUBEUSER_DEFAULT_AUTORENEW
          value: {{ .Values.authDefaults.autoRenew | quote }}
        {{- with .Values.env }}
        {{- range $key, $value := . }}
        - name: {{ $key }}
          value: {{ $value | quote }}
        {{- end }}
        {{- end }}
        ports:
        - containerPort: 9443
          name: webhook-server
          protocol: TCP
        - containerPort: 8081
          name: health
          protocol: TCP
        {{- if .Values.metrics.enabled }}
        - containerPort: {{ .Values.metrics.service.port }}
          name: metrics
          protocol: TCP
        {{- end }}
        securityContext:
          {{- toYaml .Values.securityContext | nindent 12 }}
        livenessProbe:
          {{- toYaml .Values.livenessProbe | nindent 12 }}
        readinessProbe:
          {{- toYaml .Values.readinessProbe | nindent 12 }}
        resources:
          {{- toYaml .Values.resources | nindent 12 }}
        volumeMounts:
        - mountPath: {{ .Values.webhook.certPath }}
          name: webhook-certs
        - mountPath: /tmp
          name: tmp-dir
        {{- if and .Values.metrics.enabled .Values.metrics.secure .Values.metrics.certManager.enabled }}
        - mountPath: /tmp/k8s-metrics-server/serving-certs
          name: metrics-certs
          readOnly: true
        {{- end }}
      volumes:
      - name: webhook-certs
        secret:
          secretName: {{ include "kubeuser.fullname" . }}-webhook-certs
          defaultMode: 420
      - name: tmp-dir
        emptyDir: {}
      {{- if and .Values.metrics.enabled .Values.metrics.secure .Values.metrics.certManager.enabled }}
      - name: metrics-certs
        secret:
          secretName: {{ include "kubeuser.fullname" . }}-metrics-cert
          defaultMode: 420
      {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      terminationGracePeriodSeconds: 10